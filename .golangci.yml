# Centralized Fleet golangci-lint configuration
run:
  timeout: 5m
  version: "1"

linters:
  enable:
    - staticcheck
    - govet
    - ineffassign
    - typecheck

issues:
  exclude-rules:
    - linters:
        - staticcheck
      text: "QF1008"
